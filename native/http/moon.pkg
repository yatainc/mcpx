import {
  "moonbitlang/async/http" @async_http,
  "moonbitlang/core/env",
  "yatainc/mcpx/core/http" @core_http,
  "yatainc/mcpx/core/config",
  "yatainc/mcpx/core/protocol" @mcp_client,
}

import {
  "moonbitlang/async",
  "moonbitlang/async/http" @async_http,
  "moonbitlang/async/socket",
  "moonbitlang/core/debug",
  "moonbitlang/core/json",
} for "test"

supported_targets = "native+llvm"

options(
  targets: {
    "mcp_http.mbt": [ "native", "llvm" ],
    "mcp_http_test.mbt": [ "native", "llvm" ],
    "mcp_http_cache_test.mbt": [ "native", "llvm" ],
  },
)
